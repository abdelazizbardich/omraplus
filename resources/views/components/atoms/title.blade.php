<div class="w-8/12 m-auto px-10 flex flex-col">
    @if($title)<h2 class="font-black fon text-{{$titleSize}} w-full px-10 py-3 rounded-full w-fit m-auto text-center">{{$title}}</h2>@endif
    @if($subtitle)<p class="font-medium text-xl py-3 px-4 w-fit m-auto text-center leading-relaxed">{{$subtitle}}</p>@endif
</div>